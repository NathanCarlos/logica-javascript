// const listaCompras = ['Arroz', 'Feijão', 'Farofa', 'Brócolis', 'Arroz', 'Milho', 'Arroz', 'Arroz'];

// const itemASerProcurado = prompt('Digite o nome do produto que deseja validar se está na lista de compras');
// Quero procurar onde está o Arroz
// indexOf Retorna o primeiro índice onde o item foi achado.
// const indiceItem = listaCompras.indexOf(itemASerProcurado);

// lastIndexOf Retorna o último índice onde o item foi achado.
// const ultimoIndice = listaCompras.lastIndexOf(itemASerProcurado);

// splice() remove um item através do índice.
// listaCompras.splice(ultimoIndice, 1);

// if (indiceItem > -1) alert(`O ${listaCompras[indiceItem]} está na lista!`);
// else alert('O item não está na lista!');
// console.log(indiceItem);


/**
 * Exclusão de todos os items do array com while.
 */

// const itemAserDeletado = prompt('Digite o nome do item a ser excluído');
// let indice = 0;

// while (indice < listaCompras.length) {
//     console.log(listaCompras[indice], listaCompras.length);
//     if (listaCompras[indice] === itemAserDeletado) {
//         listaCompras.splice(indice, 1);
//     }
//     indice++;
// }

// const ultimoIndice = listaCompras.lastIndexOf(itemAserDeletado);
// listaCompras.splice(ultimoIndice, 1);

// console.log(listaCompras);

// const numeros = [1,2,3,4,5];

// numeros.splice();


// console.log(numeros[numeros.length - 1]);
